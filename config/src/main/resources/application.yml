server:
  port: 9001

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/fox9916/springCloudConfig
          search-paths: /**
      lable: master

eureka:
  instance:
    hostname: config # 服务注册中心实例的主机名
  client:
    service-url:
      defaultZone: http://eureka-7001.com:7001/eureka,http://eureka-7002.com:7002/eureka

